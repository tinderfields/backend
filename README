You'll need to set up a virtual host to point at backend because the assets have absolute file references to the root.

There are two example pages index.html and login.html, the docs are coming soon.